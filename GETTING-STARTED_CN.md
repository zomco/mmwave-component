# 新手上手指南 —— 从零件到房间实时定位

[English](./GETTING-STARTED.md)

这是一份完整的首次安装教程。不需要你懂 ESPHome，也不需要焊接（只接四根杜邦线）。

做完之后，你会得到一个能报告**人在房间里的具体位置**的雷达：以你指定的某个墙角
为原点，单位厘米，在 Home Assistant 面板上实时显示。

预计耗时 45 分钟，其中大部分时间在等 Home Assistant 重启。

---

## 需要准备什么

| 物品 | 说明 |
| --- | --- |
| ESP32-C3 开发板 | 目前只构建这一个芯片系列，ESP32-S3 在计划中。 |
| 一块支持的雷达模块 | 见[型号表](./README_CN.md#雷达型号状态)。新手建议从 LD2450 开始。 |
| 4 根杜邦线 | 两块板一般都用母对母。 |
| 一根 USB-C 数据线 | 必须是**数据线**。烧录失败最常见的原因就是拿了充电线。 |
| Home Assistant | 任何安装方式都行，需要已经装好 [HACS](https://hacs.xyz/)。 |
| Chrome 或 Edge 浏览器 | 浏览器烧录必需。Firefox 和 Safari 不支持 Web Serial。 |

**不需要**：电烙铁、ESPHome 加载项、Python 环境、GitHub 账号。

---

## 第 1 步 —— 接线

四根线。两根数据线要**交叉**接：雷达的 TX 接 ESP32 的 RX，反之亦然。接反是第二常见的
失败原因，而且它不报错——设备正常启动，就是永远读不到目标。

| ESP32-C3 引脚 | 雷达引脚 | 用途 |
| --- | --- | --- |
| `5V` 或 `3.3V` | `VCC` | 供电 —— **务必查你雷达的规格书**，有的要 5 V，有的只能 3.3 V |
| `GND` | `GND` | 地 |
| `GPIO21` | `RX` | ESP32 发送 → 雷达接收 |
| `GPIO20` | `TX` | ESP32 接收 ← 雷达发送 |

> **LD2410 是例外**：它用 `GPIO4`（TX）和 `GPIO5`（RX）。其余所有支持的型号都是上表的
> GPIO21/GPIO20。

给只能接 3.3 V 的模块灌 5 V 会直接烧掉。如果规格书写得不清楚，先接 3.3 V —— 需要 5 V 的
模块顶多不响应，不会烧。

---

## 第 2 步 —— 用浏览器烧录固件

不用装任何软件，网页直接通过 USB 和板子通信。

1. 把 ESP32-C3 插到电脑上。
2. 用 Chrome 或 Edge 打开 **[在线烧录页面](https://zomco.github.io/mmwave-component/)**。
3. 在列表里选你的雷达型号。
4. 点 **Connect**，在浏览器弹出的对话框里选串口，然后点 **Install**。
5. 烧录完成后页面会提示配置 **Wi-Fi**，在这里填入你的网络名称和密码。

设备会连上 Wi-Fi 并广播自己。如果串口列表是空的，看下面的[常见问题](#常见问题)。

---

## 第 3 步 —— 在 Home Assistant 中添加设备

Home Assistant 会自动发现它。

1. 进入 **设置 → 设备与服务**。
2. 一两分钟内应该会出现 **ESPHome —— 已发现** 的卡片。点 **配置**，再点 **提交**。
3. 设备连同它的实体一起被添加：存在检测、目标坐标，以及一组校准控制项。

记下这里显示的设备名（形如 `ld2450-a4c1f8`），第 5 步要用它的实体 ID。

如果一直没发现，就手动添加：**添加集成 → ESPHome**，填入设备 IP（在路由器的客户端
列表里能查到）。

---

## 第 4 步 —— 安装可视化卡片

卡片负责把一堆数字变成房间平面图。

1. 打开 HACS 的 **前端（Frontend）**。
2. 搜索 **MMWave Radar Card**；或者把 `https://github.com/zomco/mmwave-card` 添加为
   自定义仓库，类别选 **Lovelace**。
3. 安装后 **重启 Home Assistant**。

[![Open your Home Assistant instance and open a repository inside HACS.](https://my.home-assistant.io/badges/hacs_repository.svg)](https://my.home-assistant.io/redirect/hacs_repository/?owner=zomco&repository=mmwave-card&category=plugin)

---

## 第 5 步 —— 把卡片加到面板

1. 打开任意面板，点铅笔图标，选 **添加卡片**。
2. 搜索 **MMWave Radar Card**。
3. 在下拉框里选你的雷达型号。
4. 编辑器会按型号列出需要的实体选择器，并且已经过滤好候选项，逐个填上必填项即可。

全程不用写 YAML。如果你就是想用 YAML，见
[卡片的 DIY 指南](https://github.com/zomco/mmwave-card/blob/main/DIY_CN.md)。

---

## 第 6 步 —— 校准

这一步决定了你拿到的是"雷达原始读数"还是"真实房间坐标"，也是最多人跳过的一步。
在这里花十分钟，能省掉后面几小时排查"自动化怎么在隔壁房间触发了"。

卡片有三个标签页，按顺序做。

### 标签页 ① —— 几何与边界

告诉卡片雷达装在哪。

选房间的一个墙角作为原点，用厘米量出：

- **`radar_x`** —— 沿着墙面离墙角多远
- **`radar_y`** —— 离这面墙多远
- **`radar_z`** —— 离地多高

然后拖动房间边界多边形，让它贴合你的户型。多边形之外的目标会被丢弃 —— 这就是
"隔壁邻居穿墙被检测到"的解决办法。

量的是雷达模块本身的位置，不是 ESP32 开发板。误差 ±5 cm 完全够用。

### 标签页 ② —— 偏航角校准

告诉卡片雷达朝哪。

站在一个已知位置，在平面图上标出来；再走到至少两米外的第二个位置，同样标出来。
卡片会根据这两次读数解算出朝向。

`yaw = 0` 表示雷达正对房间 **+Y** 方向，从上往下看顺时针为正。

### 标签页 ③ —— 实时验证

走动几步，看那个点跟不跟得上。跟得上就完成了。

如果点是**镜像**的（你往左走它往右），说明偏航角差了 180°；如果它和你的移动方向
成直角，就是差了 90°。这时候回标签页 ② 重做，不要手动去凑那个角度值。

点 **保存**，校准参数会写进设备，重启后依然有效。

---

## 可选 —— 多台雷达

一个房间一台雷达的话，到这里就结束了。

如果你想用多台雷达覆盖大空间或者 L 形户型，并且要一张融合后的图 + 历史记录，那还需要
装 **[mmwave-fusion](https://github.com/zomco/mmwave-fusion)** 集成 —— 它在 HACS 里是
独立的一项，类别为 **integration**。

不装也能用：卡片仍会渲染融合视图，但融合只发生在浏览器里，不保存任何数据。这种情况下
卡片会明确提示，而不是假装一切正常。

融合要求雷达能报二维位置。只测距的型号（LD2410 系列、LD2411、LD2412、LD2420、
LD2450A、LD6002、RD03E）只有距离没有方向，无法融合，编辑器也不会列出它们。

---

## 常见问题

### 烧录页面里没有串口

基本都是线的问题，换一根确定能传数据的 USB-C 线。

还不行的话，有些板子需要按住 `BOOT` 键再插 USB。Windows 偶尔要装串口驱动，根据板子
型号是 CP210x 或 CH340。

### 烧录成功，但 Home Assistant 里没有实体

多半是没连上 Wi-Fi。设备会退回自己的热点，名字是"型号 + Fallback"。用手机连上那个
热点，重新填一次网络信息。

### 有存在检测，但坐标一直是 0 或空白

TX/RX 接反了，或者模块供电不足。回第 1 步核对接线表。3.3 V 供电不够时模块会出现
"有存在、无坐标"的现象。

### 坐标乱跳，或者空房间里检测到人

毫米波能穿透石膏板和玻璃。在标签页 ① 里把边界多边形沿着房间实际轮廓收紧 —— 收紧之后
越界目标就不再触发存在检测了。

大面积金属、镜子、转动的风扇都会产生看起来像人的反射。很多时候把雷达挪半米就解决了，
怎么调参数都没用。

### 点是镜像的，或者转了个角度

偏航角不对，回标签页 ② 重做，见第 6 步的说明。

---

## 接下来看什么

| 你想做的事 | 去看 |
| --- | --- |
| 自己写 ESPHome YAML、改引脚、加传感器 | [DIY_CN.md](./DIY_CN.md) |
| 理解坐标变换和边界过滤的原理 | [DIY_CN.md —— 功能特性](./DIY_CN.md#功能特性) |
| 用 YAML 配置卡片 | [卡片 DIY 指南](https://github.com/zomco/mmwave-card/blob/main/DIY_CN.md) |
| 查某个型号的实体列表 | `docs/<型号>/README_CN.md` |
| 配置多雷达融合 | [mmwave-fusion](https://github.com/zomco/mmwave-fusion) |
